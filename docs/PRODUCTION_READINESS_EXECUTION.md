# CreatorJobs Production Readiness Execution Ledger

This ledger translates the production-readiness audit into resumable implementation work. It does not replace the audit or define new product scope. Update a row only when its status changes, and add the resolving commit only after the required tests pass.

## Ledger contract

- Source baseline: `2b616e1f239ffa5c0129be1da1b1324197d68650`
- Working branch: `integration/import-and-messaging-2026-07-30`
- Launch target: invite-only, free, India-first beta with AI Job Import retained and made production-safe.
- Allowed statuses: `NOT_STARTED`, `IN_PROGRESS`, `IMPLEMENTED`, `VALIDATED`, `BLOCKED_EXTERNAL`, `DEFERRED_WITH_RATIONALE`.
- `VALIDATED` means the implementation and its required tests passed. Existing code alone is not validation.
- `Migration`: `YES` means a database migration is expected; `POSSIBLE` means implementation inspection must decide before work begins.
- `Infra`: `YES` means production infrastructure or credentials are eventually required, even when a local adapter can validate application behavior.
- The checkpoint commit containing this file is recorded symbolically because a tracked file cannot contain its own commit hash. Resolve it with `git log --oneline -- docs/PRODUCTION_READINESS_EXECUTION.md`.

## Phase 0 — Baseline and preservation

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| BASE-001 | CRITICAL | Git/recovery | None | VALIDATED | Branch, worktree, ref, ancestry, and `git fsck` checks | NO | NO | Yes | Yes | Phase 0 checkpoint commit |
| BASE-002 | CRITICAL | Test baseline | BASE-001 | VALIDATED | Frontend, backend, migrations, builds, browser suites, lint, audits | NO | NO | Yes | Yes | Phase 0 checkpoint commit |
| BASE-003 | HIGH | Execution ledger/handoff | BASE-001, BASE-002 | VALIDATED | Document review and `git diff --check` | NO | NO | Yes | Yes | Phase 0 checkpoint commit |
| BASE-004 | HIGH | Remote/release truth | BASE-001 | BLOCKED_EXTERNAL | Authenticated fetch; remote branch and protection inspection | NO | YES | Yes | Yes | — |

## Phase 1 — Critical authentication and identity security

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| AUTH-001 | CRITICAL | Google authentication | BASE-003 | VALIDATED | Forged token, signature, issuer, audience, expiry, subject, verified-email tests | NO | YES | Yes | Yes | Phase 1A Google identity checkpoint commit |
| AUTH-002 | CRITICAL | OAuth account linkage | AUTH-001 | VALIDATED | Existing-subject and existing-email collision; no reassignment; concurrent link tests | YES | NO | Yes | Yes | Phase 1A sequential checks + Phase 1C PostgreSQL uniqueness/concurrency checkpoint |
| AUTH-003 | CRITICAL | Browser session privacy | AUTH-001 | VALIDATED | `/api/auth/session` and serialized-session leakage tests | NO | NO | Yes | Yes | Phase 1B browser-session privacy checkpoint commit |
| AUTH-004 | HIGH | Provider credentials | AUTH-003 | BLOCKED_EXTERNAL | Encryption, key rotation, redaction, hosted backfill/clear, refresh, and provider revocation tests | YES | YES | Yes | Yes | Phase 1B removed browser/JWT copies; Phase 1C-2 locally validates rotation-ready AES-256-GCM storage and migration 0055; Phase 1E adds fixed-endpoint bounded refresh/revocation, unconditional local invalidation, legacy non-feature credential cleanup, audit history, outage recovery, and redaction tests; hosted backfill/encrypted-only cutover and live Google revoke/refresh drills remain external |
| AUTH-005 | CRITICAL | Backend sessions | AUTH-001 | VALIDATED | Rotation, replay, expiry, concurrent refresh, and migration-compatibility tests | YES | NO | Yes | Yes | Phase 1C-3 persistent rotating session checkpoint commit |
| AUTH-006 | CRITICAL | Session revocation | AUTH-005 | VALIDATED | Logout, logout-all, password reset, suspension, credential-change tests | YES | NO | Yes | Yes | Phase 1C-4 validates authoritative `sid` access checks, current/all-device logout, and revocation/refresh concurrency; Phase 1C-5 validates atomic password-reset and administrator-suspension revocation, rollback, reset/login/request races, and permanent credential invalidation |
| AUTH-007 | HIGH | Browser/backend boundary | AUTH-005 | IN_PROGRESS | Cookie, CSRF/origin, server-only credential, and API compatibility tests | POSSIBLE | NO | Yes | Yes | Phase 1B moved YouTube credentials behind an authenticated same-origin route; Phase 1C-3 keeps rotating refresh credentials in the encrypted NextAuth JWT; Phase 1C-4 revokes through NextAuth's same-origin server event without serializing refresh credentials; Phase 1D-3 adds a same-origin, size-bounded, cross-site-rejecting administrator factor proxy and keeps short-lived Google reauthentication material out of the browser Session API; Phase 1E adds server-only feature-credential exchange plus cross-origin and QA rejection for YouTube refresh/disconnect; Phase 2J closes the HTTP boundary work with an enforcing nonce-based content security policy, so only the general browser access bearer remains |
| AUTH-008 | CRITICAL | Admin authentication | AUTH-005 | VALIDATED | MFA enrollment/challenge/recovery, database-backed elevation, expiry, logout, and privilege-boundary tests | YES | YES | Yes | Yes | Phase 1D-1 adds migration 0057, a global database-backed assurance gate, policy-bounded expiry, forged-claim rejection, safe logout escape paths, and fail-closed production configuration; Phase 1D-2 adds migration 0058 plus encrypted TOTP enrollment, replay-safe challenge, hash-only one-time recovery, persistent lockout, factor-change session revocation, cache-safe responses, and PostgreSQL concurrency coverage; Phase 1D-3 adds the fail-closed production browser boundary, password/Google primary reauthentication, enrollment/challenge/recovery/rotation/disable UX, server-only provider proof, one-time recovery display, and focused browser privilege/revocation validation |
| AUTH-009 | HIGH | Google/YouTube scopes | AUTH-001, AUTH-003 | BLOCKED_EXTERNAL | Basic login without YouTube; incremental consent; access-token binding; reconnect/refresh/revoke/outage tests; live provider drill | YES | YES | Yes | Yes | Phase 1E locally validates identity-only login, explicit incremental offline YouTube consent, signed OIDC `at_hash` binding, server-only exchange authorization, authoritative channel reconciliation, bounded refresh, one-retry recovery, remote revoke plus unconditional local clearing, and migration 0059 audit history; a live Google consent/reconnect/refresh/revoke/outage drill and consent-screen configuration remain external |
| AUTH-010 | HIGH | Auth abuse controls | AUTH-001, AUTH-005 | VALIDATED | OAuth and refresh throttling, enumeration, and audit tests | NO | YES | Yes | Yes | Phase 1D-2 adds bounded login/OAuth/email routes plus a durable five-attempt/15-minute administrator factor lockout and failure audits. Phase 3A makes proxy-derived caller identity trustworthy; Phase 3J makes shared Redis admission atomic and fail-closed; Phase 3K adds explicit IP policies to verification and refresh, structurally inventories every auth route, and deliberately leaves logout/logout-all unthrottled so compromise recovery cannot be stranded. Auth quota behavior is covered by the 48-case RATE focus and the 389-case affected-route matrix |

## Phase 2 — Core web security boundaries

Locally complete and certified. Every row below is `VALIDATED`; the certification totals are recorded in the handoff under "Phase 2 certification (local)". The PostgreSQL migration harness was not re-run at certification because Docker is unavailable in the working environment and Phase 2 introduced no migration — that is an environment limitation and not a passing result.

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| WEB-001 | CRITICAL | Shared outbound fetch | AUTH-005 | VALIDATED | IPv4/IPv6 private, reserved, metadata, port, proxy, timeout, size, type tests | NO | NO | Yes | Yes | Phase 2A shared outbound-fetch checkpoint: 72 primitive security tests, 327 dependent tests, and complete backend suite green |
| WEB-002 | CRITICAL | Redirect/DNS SSRF | WEB-001 | VALIDATED | Per-hop validation and DNS-rebinding/peer-verification tests | NO | NO | Yes | Yes | Phase 2A pins TCP to each validated DNS answer while retaining original TLS identity, verifies the peer, and creates a fresh cookie-free pool per redirect; escape/rebinding tests green |
| WEB-003 | CRITICAL | Organization identity fetch | WEB-001, WEB-002 | VALIDATED | Authentication, quotas, redirect and bounded-stream integration tests | NO | NO | Yes | Yes | Phase 2C moves backend hiring-identity verification (OF-005) onto the shared boundary with a per-hop platform allowlist predicate; Phase 2D-1 closes the anonymous resolver surface behind an authenticated same-origin session; Phase 2D-2 moves organization page reads to an authenticated backend endpoint returning only bounded structured fields; Phase 2D-3 removes the last Next-runtime page fetch by resolving YouTube custom paths through that endpoint. Phase 3K adds the shared Redis-backed per-user outbound allowance to organization, portfolio/oEmbed, identity-verification, URL-import and brand-enrichment entry points |
| WEB-004 | CRITICAL | Import/preview fetches | WEB-001, WEB-002 | VALIDATED | AI URL import, link preview, oEmbed, and enrichment integration tests | NO | NO | Yes | Yes | Phase 2A migrates job import/brand enrichment; Phase 2B migrates authenticated portfolio HTML and exact-provider redirect-free oEmbed, bounds each metadata field read from an untrusted page, and keeps the manual-entry fallback, with 29 focused link-preview cases, a 175-case dependent matrix, and full backend/frontend gates green |
| WEB-005 | HIGH | Internal redirects | AUTH-001 | VALIDATED | External, protocol-relative, slash, backslash, control and encoded-scheme tests | NO | NO | Yes | Yes | Phase 2F adds one canonical `lib/safeRedirect.safeInternalPath`, fixes a live open redirect where the sign-in page pushed an attacker-controlled `?next=` after successful authentication, and covers external, protocol-relative, backslash, encoded-separator, userinfo, control-character, malformed, traversal and legitimate-route cases in nine suites; every other redirect target in the tree is a literal |
| WEB-006 | CRITICAL | User-supplied URLs | BASE-003 | VALIDATED | Portfolio, profile, media, social, application, interview malicious URL tests | POSSIBLE | NO | Yes | Yes | Phase 2G adds canonical `app.core.external_url`, routes the existing `_is_http_url` chokepoint through it, and adds schema enforcement for the portfolio/profile URL fields that had none, with 36 focused cases; Phase 2H adds `lib/externalHref.safeExternalHref` at all seven render sinks so a legacy row holding an executable scheme renders inert instead of navigating, with five cases including a sink-coverage guard. Storage, render and fetch remain three separate contracts and no legacy row was rewritten |
| WEB-007 | CRITICAL | Structured data | BASE-003 | VALIDATED | `</script>`, angle, ampersand, U+2028/U+2029 JSON-LD tests | NO | NO | Yes | Yes | Phase 2E adds `lib/jsonLd.serializeJsonLd`, applies it to every inline structured-data block on the public job and talent pages, and covers tag-breakout, entity, separator, round-trip and call-site coverage in five cases |
| WEB-008 | HIGH | HTTP boundaries | WEB-005, WEB-007 | VALIDATED | CSP, Trusted Host, CORS, COOP/CORP and production header tests | NO | YES | Yes | Yes | Phase 2I adds `frame-ancestors 'none'`, COOP `same-origin-allow-popups` and CORP `same-origin` on top of the existing nosniff/Referrer-Policy/Permissions-Policy/HSTS baseline; Phase 2J adds the complete policy — `script-src 'self' 'nonce-<per-request>' 'strict-dynamic'` issued by `proxy.ts` from `lib/contentSecurityPolicy.ts`, plus `default-src`/`base-uri`/`object-src`/`form-action`/`frame-src`/`font-src`/`style-src`/`img-src`/`connect-src` — with 22 policy contract cases and a six-case browser suite that proves the framework's scripts run, an unauthorized inline script does not, and no surface reports a violation. Trusted Host and CORS remain backend concerns owned by PLATFORM-002; HSTS preload eligibility remains BLOCKED_EXTERNAL |

## Phase 3 — Dependencies, rate limiting, and request safety

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| DEP-001 | CRITICAL | Frontend dependencies | BASE-003 | IN_PROGRESS | Focused framework/auth tests, full frontend suites, production audit | NO | NO | Yes | Yes | Prior 3B/3C fixes retained. 3X patches Next16.3.1→16.3.5, Sharp0.35.3→0.35.4 (actual libheif1.23.2), baseline-browser-mapping2.10.33→2.11.24; production audit0 at every severity, no waiver. Node1304/1304; native/artifact and41 real-backend browser checks pass. Standard browser501passed/3failed; all3 Star failures reproduce on unmodified5d6de8c with oldNext16.3.1 and must be repaired before aggregate certification. Dev-tooling findings tracked separately under DEP-004 |
| DEP-002 | CRITICAL | Backend dependencies | AUTH-005 | VALIDATED | Auth/crypto tests, full backend suite, production-only audit | NO | NO | Yes | Yes | Inventory established and banked in the handoff under "DEP-002 Python vulnerability inventory": 9 production-reachable packages carried advisories. Phase 3E closed 16 advisories across six packages; Phase 3F moved cryptography 46.0.5 → 50.0.0 and closed all 11 of its advisories; Phase 3G moved ecdsa to 0.19.2, closed its DER-length denial of service and made the unpatched Minerva signing/key-generation timing issue structurally unreachable by narrowing `jwt_algorithm` to HS256/384/512; Phase 3H moved FastAPI 0.129.0 → 0.141.1 with Starlette 0.52.1 → 1.6.0 and closed 10 framework advisories, including reachable Host-header and StaticFiles findings. Phase 13B repaired the audit gate itself: `scripts.audit_production_dependencies` exports the committed `--no-dev` lock with hashes, audits that artifact with pinned pip-audit 2.10.1, and compares every finding with an exact package/version/advisory/evidence allowlist. Any new, changed, unanalysed or stale finding fails. The RATE-001 slice adds the maintained redis-py 8.1.0 runtime client and re-runs that gate: one analysed ecdsa residual and zero unanalysed findings; frontend production audit remains zero high/critical. No advisory is ignored and no severity threshold is used. The complete backend is now 7,499 passed/65 skipped |
| DEP-003 | HIGH | Production dependency set | DEP-002 | BLOCKED_EXTERNAL | Clean production sync/build and SBOM inspection | NO | YES | Yes | Yes | The production image installs from the committed `uv.lock` with `uv sync --locked --no-dev`; redis-py 8.1.0 expands the current-Python isolated production set from 52 to 53 installed packages and the hashed backend export from 53 to 55 requirement rows (including conditional requirements). Phase 13B generated and inspected both production dependency inventories; frontend CycloneDX remains 39 components. Source-level container hardening is 22/22. No locally implementable dependency-set work remains, but an actual immutable-image build and image/SBOM scan require a functioning Docker daemon/scanner environment; the local Docker CLI could not reach a daemon and Gitleaks/Trivy/Syft are unavailable. Those executions remain external rather than being inferred from source checks |
| RATE-001 | CRITICAL | Redis rate limiter | DEP-002 | VALIDATED | Production boot, atomic quota, expiry and fail-closed tests | NO | YES | Yes | Yes | `security(rate-limit): make Redis admission atomic` replaces the cross-instance read-then-write race with one Redis Lua `EVAL` using Redis server time, a unique member, expiry and one linearized allow/reject decision. The shipped redis-py client has bounded connect/command timeouts and an explicit RESP2 contract; startup pings the configured shared backend and refuses service when it is unavailable, request-time failure returns a privacy-safe 503, and there is never an automatic process-local fallback. Unit/protocol/security tests cover 100-way contention, key isolation, malformed replies, safe outage, startup and lifecycle. A standalone exercise used eight independent clients and 200 contenders against verified official Redis 7.4.11 source on loopback: exactly 17 allowed/183 rejected, with real key cardinality/TTL, isolation, fail-closed outage and recovery all passing twice. The same loopback-only exercise is pinned to exact `redis:7.4.11-alpine` in CI. Managed Redis provisioning, credentials, alert delivery, failover and a production cutover drill remain external under CERT-002/OPS-007 rather than making this code item incomplete |
| RATE-002 | CRITICAL | Trusted client IP | WEB-008 | VALIDATED | Spoofed and trusted proxy-chain tests | NO | YES | Yes | Yes | Phase 3A replaces unconditional `X-Forwarded-For` trust with `client_identity`, which believes a forwarded header only when the peer is a configured proxy and then reads the chain right to left. Taken before RATE-001 deliberately: the defect is live under the current in-memory backend too, and identity derivation does not depend on where the counters live. 22 focused cases plus a production configuration decision |
| RATE-003 | HIGH | Endpoint quotas | RATE-001 | VALIDATED | Auth, upload, preview, location, search, messaging, admin quota tests | NO | YES | Yes | Yes | Phase 3K (`security(rate-limit): enforce user-scoped endpoint quotas`) centralizes safe 429/503 enforcement; namespaces IP and verified-user buckets; adds shared category ceilings for verification/refresh, outbound fetches, media uploads, public search and administrator requests; converts existing authenticated marketplace/job/import/review limits from shared-NAT IP identity to verified user identity; covers every messaging mutation and every admin/support route; and adds a structural policy inventory plus behavioral isolation/authorization tests. Phase 3L closes the only remaining surface: Google Places autocomplete/details now execute only in authenticated backend routes under one shared 120/minute verified-user Redis allowance, and bounded `Retry-After` guidance survives the Next proxy. The 48-case quota/identity focus, 389-case affected-route matrix, final 37-case Places/quota backend suite and five-case frontend boundary suite pass |
| RATE-004 | HIGH | Request safety | None for body bounds; concurrency parts need RATE-001 | IN_PROGRESS | Body-size, timeout, concurrency, cancellation and recovery tests | NO | YES | Yes | Yes | Phase 3I closes the body-size half: a pure-ASGI `RequestBodyLimitMiddleware` bounds every inbound body before parsing, refusing an oversized declared `Content-Length` without reading a byte and counting streamed bytes so omitting the header buys nothing. Registered innermost so its 413 still carries CORS headers and `X-Request-ID`. Ceilings derived from evidence — 12 MiB for the two media endpoints (an 8 MiB banner is ~10.7 MiB as base64) and 2 MiB elsewhere — with exact-path policy so a crafted path cannot claim the larger allowance. Phase 3L closes OF-105 with application-level input/field ceilings, a 128 KiB decoded JSON ceiling, redirect refusal, proxy/cookie bypass, per-operation HTTP timeouts and a six-second whole-attempt deadline. Phase 3M closes OF-103/OF-104: both YouTube channels/videos callers now use one backend-only fixed-provider client with bounded selectors, strict 512 KiB decoded JSON, redirect/proxy refusal, whole-resolution deadlines, privacy-safe errors and narrow non-destructive OAuth reauthentication classification; the authenticated Next resolver has no key/provider network code and falls back to URL-derived identity. Phase 3N adds bounded non-queuing per-worker HTTP admission with cancellation/response-cleanup slot ownership and a separate bounded liveness reserve (RATE-004A validated). Phase 3O adds idle/cumulative incomplete-body receive deadlines and preserves 408/413 through the FastAPI parser (RATE-004B validated). Handler/DB deadlines and ingress/capacity validation remain, so RATE-004 stays IN_PROGRESS |
| RATE-005 | CRITICAL | AI resource guards | RATE-001 | IN_PROGRESS | Per-user burst/daily/concurrency/input/cost guard tests | POSSIBLE | YES | Yes | Yes | Daily counter, authenticated burst limit and bounded input/output retained. 3Q fences stale cleanup; 3R aligns provider/lease deadlines; 3S adds cross-draft user admission; 3W adds shared rolling 30-day attempt admission through the existing atomic Redis limiter (AI-004A). Actual PostgreSQL contention, durable execution and production budget-history persistence remain unverified |

Phase 3M–3P sub-ledger (2026-09-15): specific outbound/admission/body/database boundaries, not all of RATE-004. LAST full-backend run is Phase 3O: 7680 total = 7615 passed +65 skipped, zero failures/errors (exit0, parsed JUnit and independent collection). Phase 3P current collection is7704 (+24); focus195 passed/1 skip, compatibility92 passed; not a new full-suite execution. Real PostgreSQL timeout/recovery, handler and production ingress/capacity proof remain under RATE-004.

| ID | Severity | Phase | Subsystem | Dependencies | Status | Tests required / result | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| OF-103 | HIGH | 3M | Backend YouTube provider | RATE-003 | VALIDATED | 43 boundary cases plus auth/quota/channel compatibility: 104 passed; broader compatibility 169 passed/1 existing skip; sessions 16 passed. Fixed destinations, deadlines/body/field bounds, no credential redirects, malformed snapshot authority preservation and OAuth classification | NO | NO for local proof; YES for live key restriction/drill | Yes | Yes | `security(provider): consolidate bounded YouTube lookups` |
| OF-104 | HIGH | 3M | Next YouTube identity proxy | OF-103 | VALIDATED | Resolver unit 11/11; full Node 1297/1297; TypeScript/build/lint green; real-backend organization resolver 7/7 including anonymous refusal and absent-provider YouTube fallback | NO | NO for local proof; YES for backend key cutover | Yes | Yes | `security(provider): consolidate bounded YouTube lookups` |
| RATE-004A | HIGH | 3N | HTTP resource admission | RATE-001 (account quota remains separate) | VALIDATED | 15 admission cases: 100-way contention, immediate rejection, cancellation/send cleanup, exact bounded liveness reserve and headers; focus 195 passed/1 existing skip; complete backend 7596 passed/65 skipped; Node 1297/1297; real-backend CSP/resolver/persona browser matrix 42/42 | NO | NO for local proof; YES for per-worker capacity/ingress measurement | Yes | Yes | `security(request): bound in-flight HTTP admission` |
| RATE-004B | HIGH | 3O | Incomplete-body receive deadlines | RATE-004A | VALIDATED | 17 added body regressions plus 2 configuration cases; focus 214 passed/1 existing skip; full backend 7615 passed/65 skipped; Node 1297/1297; build/TSC/lint/Ruff green; real-backend browser matrix 42/42. No handler/commit cancellation timeout imposed | NO | NO for local proof; YES for ingress and slow-network media validation | Yes | Yes | `security(request): bound incomplete body receive waits` |
| RATE-004C | HIGH | 3P | Asyncpg operation deadlines | Existing pool bounds | BLOCKED_EXTERNAL | Actual-driver stalled-loopback handshake and timeout forwarding, finite config and safe drill-target tests; focus195 passed/1 skip, compatibility92 passed, Node1298/1298. Real PostgreSQL command timeout/rollback/pool recovery still required; read-only drill wired into CI/local harness but not run here | NO | YES: disposable PostgreSQL for remaining proof | Yes | Yes | `security(database): bound asyncpg connection and command waits` (implementation, not final validation) |

Phase3X security patch group (2026-09-17): package-lock changes are limited to Next/SWC, Sharp/libvips platform artifacts and browser-mapping data. No application workflow changed. Parent DEP-001 remains IN_PROGRESS until broad browser certification is green.

| ID | Severity | Phase | Subsystem | Dependencies | Status | Tests required / result | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| DEP-001A | CRITICAL | 3X | Production framework/image patches | DEP-001 | VALIDATED | Known RCE/decoder/termination version floors; actual libheif; benign AVIF resize; child-process survival; no advisory exceptions. Focus16/16; all Node1304/1304; TSC/build/lint pass; real-backend CSP/persona/import41/41. Clean production-only artifact10/10 and audit0 vulnerabilities. Standard aggregate's3 failures proven pre-existing, not accepted as a green aggregate | NO | NO for local artifact; live image scanner remains external | Yes | Yes | `security(deps): patch framework and image processing advisories` |
| DEP-004 | HIGH | 3Z complete | Developer/build tooling dependencies | DEP-001A | VALIDATED | Compatible Babel/humanfs/brace-expansion/Browserslist/YAML update clears all5 package findings; full npm audit0 all severities, no exceptions. New security11/11; full Node1315/1315; TSC/lint/build pass; clean production artifact10/10/audit0. Only shared caniuse-lite data also changes in production; all5 affected packages remain dev-only. Full browser recertification still belongs to CERT-001 | NO | NO | Yes for reproducible secure release tooling | Yes | `security(deps): patch developer tooling advisories` |
| CORRECT-002A | HIGH | 3Y complete | Authenticated demo Star boundary | CORRECT-002 existing accepted behavior | VALIDATED | Explicit demo uses liveMode to gate backend resolution/write despite a session token. Serial3/3 and full standard504/504 with2workers; no-network assertions cover keyboard/row/header/card. Live Star2/2 proves private durability and actual503 rollback. Node1304/1304; TSC/build/lint green. Initial5worker7 unrelated intermittent failures retained in handoff; all7 passed isolated and full2worker, not erased from certification risk | NO | NO | Yes | Yes | `fix(workspace): keep authenticated demo stars local` |

## Phase 4 — Deterministic product correctness and trust

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| CORRECT-001 | HIGH | Public profiles | WEB-006 | NOT_STARTED | Recruiter/agency routes and entry-link browser tests | NO | NO | Yes | Yes | — |
| CORRECT-002 | HIGH | Pipeline/messaging | AUTH-005 | NOT_STARTED | Pipeline-to-dock, lifecycle, two-persona and duplicate-delivery tests | POSSIBLE | NO | Yes | Yes | — |
| CORRECT-003 | HIGH | Search | BASE-003 | NOT_STARTED | Result, empty, error, filter and route-state browser tests | NO | NO | Yes | Yes | — |
| CORRECT-004 | CRITICAL | Applications | AUTH-005, WEB-006 | NOT_STARTED | Internal/external/expired/closed/idempotent cross-persona tests | POSSIBLE | NO | Yes | Yes | — |
| CORRECT-005 | HIGH | Job posting | BASE-003 | NOT_STARTED | Step headings, reopen, role, location, narrow-mobile and publish tests | POSSIBLE | NO | Yes | Yes | — |
| CORRECT-006 | HIGH | Duplicate UI identity | BASE-003 | NOT_STARTED | Single accessible Apply action per viewport; deterministic locators | NO | NO | Yes | Yes | — |
| CORRECT-007 | MEDIUM | Workspace performance | CORRECT-002 | VALIDATED | Bounded list-wide requests and no per-row request amplification | YES | NO | Yes | Yes | Completed by the navigation, batched-summary, concurrent-identity and `perf(activity): paginate the workspace timeline` slices. `/me/activity/summary` now keyset-pages one deterministic viewer-scoped union before loading wide rows, with a cursor-bound snapshot, source/UUID tie-breakers, exact four-source totals and an owned-only deep-link anchor. The client requests only its active workspace, merges pages without rolling back local/realtime state, and labels search/queue/stage results as loaded-only until history is complete. Backend broad 65/65, disposable PostgreSQL 29/29 with migration cycle, frontend 1,268/1,268, workspace paging 11/11 and Pipeline 31/31 validate the contract; the 100-session loopback exercise remains a correctness/security assertion rather than a capacity claim |
| CORRECT-008 | MEDIUM | Assistant layout | BASE-003 | NOT_STARTED | Typing indicator ordering, scroll bounds, mobile/desktop layout tests | NO | NO | Yes | Yes | — |
| TRUST-001 | CRITICAL | Compensation display | BASE-003 | NOT_STARTED | Preserve source amount/currency; flexible/unknown/unpaid tests | POSSIBLE | NO | Yes | Yes | — |
| TRUST-002 | CRITICAL | Marketing claims | BASE-003 | NOT_STARTED | Copy inventory and evidence-link assertions | NO | NO | Yes | Yes | — |
| TRUST-003 | HIGH | Marketplace metrics | BASE-003 | NOT_STARTED | Defined view/save/interest/response semantics and bot/dedup tests, or hidden-state tests | POSSIBLE | POSSIBLE | Yes | Yes | — |
| PRODUCT-001 | HIGH | Job alerts | BASE-003 | NOT_STARTED | Beta UI/API absence; later durable subscription/delivery tests before re-enable | POSSIBLE | YES | Yes | Yes | — |
| PRODUCT-002 | HIGH | Pricing/entitlements | BASE-003 | NOT_STARTED | Beta route/API policy, no client-created entitlement, truthful-copy tests | POSSIBLE | YES | Yes | Yes | — |
| PRODUCT-003 | MEDIUM | Incomplete integrations | AUTH-009 | NOT_STARTED | Unsupported controls absent; supported connect/disconnect paths tested | NO | YES | Yes | Yes | — |
| PRODUCT-004 | HIGH | Seed/demo isolation | BASE-003 | NOT_STARTED | Production database/content and fixed-ID dependency checks | NO | YES | Yes | Yes | — |

## Phase 5 — Durable transactional email and invite-only beta

Locally complete and certified. Every row below is `VALIDATED` except EMAIL-005, which is `BLOCKED_EXTERNAL` on a sending domain and a paid provider; the certification totals are recorded in the handoff under "Phase 5 certification". No PostgreSQL migration harness was run because Docker is unavailable in this environment — migrations 0060/0061/0062 render both upgrade and downgrade offline, which is not the same thing and is not claimed to be.

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| INVITE-001 | CRITICAL | Invitations | AUTH-001, AUTH-005 | VALIDATED | Email binding, expiry, single use, revocation, concurrency and audit tests | YES | NO | Yes | Yes | b141919, then "feat(invites): check the invitation at both signup doors" for atomic single use |
| INVITE-002 | CRITICAL | Registration policy | INVITE-001 | VALIDATED | Google and email/password enforcement; existing-user compatibility tests | NO | NO | Yes | Yes | "feat(invites): check the invitation at both signup doors" |
| EMAIL-001 | CRITICAL | Outbox worker | DEP-002 | VALIDATED | Lease, concurrent claim, retry, crash recovery and idempotency tests | YES | YES | Yes | Yes | 95a1a66, ebaf659, 5e0ded7, 5a7725f |
| EMAIL-002 | CRITICAL | Authentication email | EMAIL-001 | VALIDATED | Invite, verification, reset, expiry and delivery-failure tests | NO | YES | Yes | Yes | "feat(email): make auth mail a promise the database keeps" — verification and reset queue durably and a runner drains them; the invitation email waits on an issuing endpoint |
| EMAIL-003 | HIGH | Event email | EMAIL-001 | VALIDATED | Application/hiring/safety notification state and retry tests | NO | YES | Yes | Yes | "refactor(email): leave one way for an email to be delivered" — the enqueue was already correct; this removed the second delivery path and proved queued-to-sent and the retry end to end |
| EMAIL-004 | HIGH | Bounce/suppression | EMAIL-001 | VALIDATED | Provider webhook signature, bounce, complaint and suppression tests | YES | YES | Yes | Yes | "feat(email): stop mailing an address that told us to stop" — migration 0062; signature scheme is provider-neutral until EMAIL-005 picks one |
| EMAIL-005 | HIGH | Domain authentication | EMAIL-001 | BLOCKED_EXTERNAL | Provider staging send, SPF, DKIM, DMARC and bounce-domain verification | NO | YES | Yes | Yes | — |

## Phase 6 — AI Job Import production hardening

Phase 3Q reinspection corrects the earlier local-completion claim: the HTTP process route still awaits provider work, and the recovery sweeper never executes queued work. Durable execution therefore remains locally implementable, not merely an external multi-worker proof. The daily counter and per-draft lease do not supply cross-draft per-user concurrency. System-wide attempt/token budgets can be implemented without inventing model prices; a currency budget still needs configured commercial inputs. AI is retained: source -> interpretation -> canonical draft -> Post Job -> human review -> publication. Historical Phase 6 tests remain evidence only for the contracts they exercised.

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| AI-001 | CRITICAL | Durable import queue | RATE-001, WEB-004 | IN_PROGRESS | Enqueue, lease, retry, crash recovery, cancellation and queue outage tests | YES | NO | Yes | Yes | Existing0063 lease/attempt state, atomic claims and stranded-row sweep are preserved. 3Q confirms API still awaits process(); sweeper explicitly calls no provider. Owned durable execution worker, enqueue/poll compatibility and queue-outage proof remain LOCAL work; actual PostgreSQL multi-worker race remains separately unverified. Fenced cleanup is AI-001A below |
| AI-002 | CRITICAL | Import idempotency | AI-001 | VALIDATED | Repeated request, worker retry, duplicate charge/job and concurrent request tests | NO | NO | Yes | Yes | Creation idempotency (client_request_id) PREDATES this work and is tested in test_job_import_readiness.py: concurrent duplicate source and draft requests return one record. AI-001 added the expensive half — a repeat after success calls no provider and consumes no attempt, and two concurrent process requests produce one call. No new migration: reusing the existing identifier rather than inventing a second one |
| AI-003 | CRITICAL | Quotas/concurrency | RATE-005, AI-001 | IMPLEMENTED | Daily, burst, concurrent, input-size and reset-window tests | Existing0064 only | NO | Yes | Yes | 0064 atomic daily counter; body/source/output bounds; authenticated burst limit. 3S now admits at most JOB_IMPORT_CONCURRENCY_LIMIT live leases across all of an account's drafts in the same quota-write/claim transaction; rejected work rolls back quota and attempt, no provider call. Local six-request race, account isolation, hidden-draft/expiry and transaction ownership tests pass. PostgreSQL cross-process proof remains unverified under AI-003A |
| AI-004 | CRITICAL | Budget/cost controls | AI-001 | IN_PROGRESS | Model allowlist, token/output cap, request/system budget and threshold tests | NO | YES | Yes | Yes | Allowlist/output/deadline caps and per-user quotas retained. AI-004A adds one system-wide rolling 30-day logical extraction allowance, including bounded internal retries, not currency or tokens. Currency budgets require authoritative configured prices/billing reconciliation; do not invent them. Managed Redis persistence/no-eviction/recovery and provider-account billing limits remain operator gates |
| AI-005 | HIGH | Provider resilience | AI-001 | VALIDATED | Bounded timeout/retry, unsupported model, unavailable provider and recovery tests | NO | NO | Yes | Yes | Already implemented before this phase and verified by survey: measured 90s timeout with a 45s viable floor, ONE retry for transient failures only (never schema/auth/refusal), retry-after honoured. Unsupported model now refused at provider construction. Recovery/abandonment covered by test_job_import_terminal_state and test_job_import_provider_technical_recovery, and now by the lease |
| AI-006 | HIGH | Boot/readiness | AI-001 | VALIDATED | Missing key, queue, model and provider readiness tests | NO | NO | Yes | Yes | Credential, model and queue-recovery readiness on GET /api/v1/health/job-import. Provider health is deliberately NOT probed: a readiness check that called the provider would bill a request every time a load balancer looked |
| AI-007 | CRITICAL | Human review | AI-001 | VALIDATED | Draft-only output, canonical Post Job validation, no auto-publish tests | NO | NO | Yes | Yes | Architectural, and predates this phase: no import route can publish, can_publish_directly is asserted False across test_job_import_readiness / test_job_import_review_fixture / test_openai_job_import, and test_import_prompt_injection proves publication state is not a field an import can write. The durable queue changed nothing here — the sweep deliberately produces no draft at all |
| AI-008 | CRITICAL | Import SSRF | WEB-004, AI-001 | VALIDATED | URL import inherits complete outbound-fetch security suite | NO | NO | Yes | Yes | job_url_fetcher goes through SafeOutboundFetcher (Phase 2A/2B), so URL import inherits DNS-pinning, public-IP-only, per-redirect revalidation, bounded bodies and proxy-ignoring by construction. Inventory in docs/PRODUCTION_READINESS_OUTBOUND_FETCH.md |
| AI-009 | HIGH | Provider privacy | AI-001 | VALIDATED | Payload minimization, log redaction, `store` configuration and retention-copy tests | NO | NO | Yes | Yes | The payload was already minimal (source text + schema + rules; no account, email, session or internal ids). tests/test_job_import_provider_privacy.py PINS it: the permitted field sets are enumerated so the payload fails when it GROWS, extra="forbid" is proven to reject smuggled identity, and store=False is asserted in the adapter |
| AI-010 | HIGH | Evaluation corpus | AI-001 | IMPLEMENTED | Literal, inference, currency, ambiguity, conflict, missing, malformed, injection, long-input fixtures | NO | NO | Yes | Yes | A large corpus predates this phase: test_job_import_inference, _intelligence_matrix, _semantic_containment, _source_corpus, _experience_semantics, _weekly_hours, _location_resolution, and test_import_prompt_injection for the hostile cases. Not re-derived here; no gap found by survey |
| AI-011 | CRITICAL | Kill switch | AI-001 | VALIDATED | Runtime disable, in-flight behavior, UI message, audit and recovery tests | NO | NO | Yes | Yes | "feat(import): give AI job import a switch that actually turns it off" — JOB_IMPORT_ENABLED, default true; taken out of its AI-001 dependency because a kill switch is worth more before a durable queue than after |

Phase 3Q–3V lease/provider sub-ledger:

| ID | Severity | Phase | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| AI-001A | CRITICAL | 3Q | Attempt-owned lease cleanup | Existing0063 lease | VALIDATED | Baseline regression erased the newer lease; five new fencing cases now pass. Focus174 passed; broader AI5632 passed/39 existing skips; Node1298/1298; canonical import browser6/6 plus build. Both cleanup paths match the full attempt UUID; no schema or API change | NO | NO | Yes | Yes | `security(ai-import): fence cleanup by processing attempt` |
| AI-005A | CRITICAL | 3R | Provider wall-clock/lease coherence | AI-001A | VALIDATED | Shared viable floor/backoff budget; owned provider deadline, cancellation/late-result rejection, source-preserving504/retry and configured-maximum lease tests. Final AI5658 passed/39 skips; browser6/6; Node1298/1298. Complete backend candidate7669 passed/65 skips before final late-result guard, then entire affected AI matrix rerun | NO | NO | Yes | Yes | `security(ai-import): align provider deadlines and durable leases` |
| AI-003A | CRITICAL | 3S | Cross-draft user concurrency | AI-001A, AI-005A, existing0064 counter | BLOCKED_EXTERNAL | Local code complete: six contenders exactly2 provider calls/4 uncharged429s; independent accounts; hidden/deleted drafts still count; expiry/retry; same admission transaction and no provider-held transaction. Focus139 passed/1 skip; configuration157 passed/1 skip; AI5669 passed/39 skips; browser6/6; Node1298/1298. Actual PostgreSQL contention proof still required | NO | YES: disposable PostgreSQL for remaining proof | Yes | Yes | `security(ai-import): limit concurrent drafts per account` (implementation, not final PostgreSQL validation) |
| AI-002A | HIGH | 3T | Contested-claim snapshot/idempotent response | AI-002 | VALIDATED | Barrier reproduces both requests reading awaiting_processing before either admission; baseline returns409. Opt-in fresh owner-scoped read now returns200 already_processing, one provider call/one charge. Ordinary pending edits and foreign/deleted boundaries preserved. Focus55 passed; AI5672 passed/39 skips; Node1298/1298; browser6/6 | NO | NO for this local snapshot regression | Yes | Yes | `fix(ai-import): refresh state after contested claims` |
| AI-003B | HIGH | 3U | Owned PostgreSQL admission exercise | AI-003A | IMPLEMENTED | CLI target refusal, eight-session quota/claim/count race, duplicate claims, account isolation, reusable capacity and exact-UUID cleanup; local exercise/guard focus57 passed; compatibility77 passed; Node1299/1299. CI/local PostgreSQL harness wired, but actual PostgreSQL execution NOT run; AI-003A remains BLOCKED_EXTERNAL | NO | YES for actual PostgreSQL proof | Yes | Yes | `test(ai-import): add owned PostgreSQL admission drill` |
| AI-001B | CRITICAL | 3V | Recovery eligibility/attempt preservation | Existing0063 lease | VALIDATED | Three baseline regressions: untouched drafts failed, recovery spent attempts, final-attempt crash unrecoverable. Recovery now requires an expired lease, spends no attempt and ignores settled failures. Six added cases; focus115 passed; AI5687 passed/39 skips; Node1299/1299; browser6/6 | NO | NO for local recovery contract | Yes | Yes | `fix(ai-import): recover only abandoned leased attempts` |
| AI-004A | CRITICAL | 3W | Shared system attempt allowance | RATE-001, AI-003A local implementation | VALIDATED | Existing atomic Redis limiter; cross-account cap, concurrent protocol calls, rolling expiry/limit changes, no duplicate charge, rollback on refusal/outage/cancellation, conservative uncertain reservations, readable completed drafts and retained HTTP write gates. Focus155 passed/1 skip; browser6/6; Node1299/1299; full backend7712 passed/65 skips,7777 JUnit cases with0failures/errors matching collection. Pre-existing metrics fixture reproduced on f351818, repaired without weakening assertions | NO | YES for production Redis persistence/no-eviction/recovery | Yes | Yes | `security(ai-import): bound shared system attempt allowance` |

## Phase 7 — Durable media storage

Locally complete for everything the environment can hold. What remains is honest rather than hidden: MEDIA-002 is deferred with rationale (grants and quarantine protect a direct-to-bucket architecture that does not exist here — every byte already passes server-side validation before storage), the real object-store adapter needs a bucket, credentials and a bill, and MEDIA-005 still owes account-deletion cascade and an orphan sweep, both of which want a stored key column. Certification totals are in the handoff under "Phase 7 certification".

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| MEDIA-001 | CRITICAL | Storage abstraction | WEB-006 | IMPLEMENTED | Local adapter, object-key ownership and provider adapter contract tests | NO | YES | Yes | Yes | "feat(media): put uploads behind a seam the filesystem does not own" — MediaStorage protocol, LocalMediaStorage, owner-scoped unguessable keys validated at every entry point, fake adapter proves substitutability. The real object-store adapter needs a bucket, credentials and a bill: BLOCKED_EXTERNAL |
| MEDIA-002 | CRITICAL | Upload grants/quarantine | MEDIA-001, RATE-003 | DEFERRED_WITH_RATIONALE | Grant expiry/scope, finalize, unauthorized access and cleanup tests | YES | YES | Yes | Yes | Grants and quarantine exist so bytes that reach storage WITHOUT passing through the application can be held until they are checked. Uploads here do not work that way: they arrive base64 in an authenticated request, and the server validates every byte (MEDIA-003) BEFORE calling storage.put — verified at profile_service lines ~1998/2017 and ~2074/2093. The property quarantine protects therefore already holds. Building a grant flow now would be designing for a direct-to-bucket architecture that does not exist and cannot be tested here; it becomes READY when the object-store adapter lands (BLOCKED_EXTERNAL) |
| MEDIA-003 | CRITICAL | Image validation | MEDIA-002 | IMPLEMENTED | Magic byte, decode, pixel/dimension/byte, malformed and metadata-strip tests | NO | NO | Yes | Yes | "feat(media): decide what an upload is from its bytes, not its label" — magic-byte sniffing, declared-vs-actual mismatch refused, pixel/dimension caps, EXIF/XMP/text stripping for PNG/JPEG/WebP. RE-ENCODING is NOT done: it needs an imaging library, which is a dependency decision. GIF metadata is passed through — recorded, and GIFs carry no EXIF |
| MEDIA-004 | HIGH | Canonical delivery | MEDIA-001 | IMPLEMENTED | Canonical/CDN URL and host-header poisoning tests | NO | YES | Yes | Yes | "fix(media): stop letting a request decide where media lives" — stored URLs now come from MEDIA_PUBLIC_BASE_URL, which production refuses to boot without. Was a STORED host-header poisoning defect: request.base_url comes from the Host header and was persisted into avatar_url. A CDN hostname is a configuration value for that setting; the CDN itself is external |
| MEDIA-005 | HIGH | Object lifecycle | MEDIA-002 | IN_PROGRESS | Replacement, orphan, deletion, retry and retention tests | NO | YES | Yes | Yes | "feat(media): stop keeping the avatar someone replaced" — replacement now deletes the superseded object, resolved from the stored URL and refused if it does not look like a key we wrote. Deletion after commit and never able to fail the upload. STILL OPEN: account-deletion cascade and a sweep for objects orphaned by an earlier crash, both of which want a stored key column rather than URL parsing |

## Phase 8 — Realtime and scalable shared state

Locally complete and certified. The architecture was already the one this phase asks for — events are emitted after the database commit, authorization is per recipient, and the HTTP API stays authoritative — so the work was the seam a second instance plugs into, not a rewrite. What cannot be proven here is named: the broker adapter and real cross-process delivery need a broker (`BLOCKED_EXTERNAL`), and presence has no product surface to give TTL semantics to. Certification totals are in the handoff under "Phase 8 certification".

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| REALTIME-001 | HIGH | Shared event bus | RATE-001 | IMPLEMENTED | Cross-instance conversation/notification delivery and deduplication tests | NO | YES | Yes | Yes | "feat(realtime): give events a seam a second instance can plug into" — RealtimeBus seam, event envelope with a required id, per-connection at-least-once deduplication, publish failure never fails the write, and production REFUSES the process-local bus unless explicitly acknowledged. The Redis/NATS adapter itself and real cross-process delivery proof are BLOCKED_EXTERNAL |
| REALTIME-002 | HIGH | Ephemeral presence | REALTIME-001 | VALIDATED | Typing/presence TTL, disconnect, reconnect and instance-switch tests | NO | NO | Yes | Yes | Typing already had a 6s expiry, disconnect cleanup and multi-tab handling BEFORE this phase; now pinned by tests so it cannot be removed quietly. PRESENCE HAS NO PRODUCT SURFACE — no endpoint, model or UI exposes online status — so there is nothing to give TTL semantics to. Adding one would be inventing a feature, which is a product decision, not a hardening task |
| REALTIME-003 | HIGH | Degraded operation | REALTIME-001 | IMPLEMENTED | Redis unavailable/recovery and durable HTTP reconciliation tests | NO | NO | Yes | Yes | "feat(realtime): keep the record when the hint does not arrive" — a totally broken bus is driven through the real message route: the message still commits, still reads back over HTTP (which IS the reconnect path), and still counts as unread. GET /api/v1/health/realtime distinguishes configured from cross_instance and leaks no configuration. Real broker outage/recovery against an actual broker remains BLOCKED_EXTERNAL |

## Phase 9 — Privacy, legal mechanics, support, and moderation

Locally complete and certified for everything that does not require a decision nobody in this repository has authority to make. Deliberately open, and named rather than rounded up: erasure and every retention duration (`BLOCKED_PRODUCT_DECISION`), final legal wording and counsel approval (`BLOCKED_EXTERNAL`, LEGAL-002), export delivery mechanics, and the Terms/Privacy version-marker decision recorded in `docs/LEGAL_SURFACE_INVENTORY.md`. Certification totals are in the handoff under "Phase 9 certification".

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| PRIV-001 | CRITICAL | Legal acceptance | AUTH-005 | IMPLEMENTED | Versioning, acceptance, reacceptance and audit tests | YES | NO | Yes | Yes | "feat(legal): record which version someone agreed to, not that they agreed" — migration 0065, one row per (user, document, version), idempotent by unique constraint, superseded acceptance counts for nothing. Versions live in CODE so a running server cannot reference wording it does not ship. The document TEXT is LEGAL-001 and remains external; the API surface that presents outstanding documents is the next slice |
| PRIV-002 | CRITICAL | Data export | PRIV-001, MEDIA-001 | IN_PROGRESS | Reauthentication, complete archive, authorization and expiry tests | NO | NO | Yes | Yes | "feat(privacy): export what someone owns, and nothing that is not theirs" — build_export() with an EXPLICIT hand-written shape (no reflection), messages filtered to the ones they sent, no credentials. STILL OPEN: the delivery mechanism (reauthentication, an expiring download link, async archive generation), which is what the remaining test names describe |
| PRIV-003 | CRITICAL | Account deletion | PRIV-001 | IN_PROGRESS | Immediate hiding, suspension, anonymization/deletion, retry and exception tests | YES | YES | Yes | Yes | "feat(privacy): let someone ask to be deleted, and act on the part that is theirs" — migration 0067. Request recorded, account hidden immediately, every session revoked, cancellable. ERASURE AND RETENTION DURATION ARE NOT BUILT: the period is a legal/product decision (BLOCKED_PRODUCT_DECISION) and inventing one would look exactly like a decided one. The erasure/anonymisation pass becomes READY once a duration exists |
| PRIV-004 | HIGH | Retention/legal holds | PRIV-003 | NOT_STARTED | Policy scheduler, hold precedence, release and audit tests | YES | YES | Yes | Yes | — |
| PRIV-005 | HIGH | Compliance admin | PRIV-002, PRIV-003, PRIV-004 | NOT_STARTED | Queue authorization, status, evidence, appeal and audit tests | YES | NO | Yes | Yes | — |
| PRIV-006 | HIGH | Notification consent | EMAIL-003 | IMPLEMENTED | Preference, unsubscribe, essential-message and suppression tests | YES | NO | Yes | Yes | "feat(consent): let people switch off the mail we chose to send them" — migration 0066, opt-OUT rows (absence = subscribed), essential/optional asymmetry mirroring the Phase 5 suppression rule, enforced in the worker at send time. The one-click unsubscribe LINK (signed token, no login) is the next slice; preferences are enforced today and settable through the service |
| MOD-001 | HIGH | Profile/portfolio moderation | PRIV-005 | IMPLEMENTED | Hide/restore, reason, authorization, notification and audit tests | NO | NO | Yes | Yes | "fix(moderation): enforce both blocking states everywhere, not just the one in mind" — enforcement-hardening rather than a new product, per scope decision. Every blocking/visibility read now goes through account_block()/account_is_blocked(); a structural test forbids new direct suspended_at reads outside the admin suspend endpoints. Fixed three real gaps: a deletion-hidden account could still LOG IN, keep a PUBLIC PROFILE, and keep a public talent listing |
| MOD-002 | HIGH | Audit-log integrity | PRIV-005 | IN_PROGRESS | Append-only, retention, export and ordinary-admin mutation denial tests | NO | NO | Yes | Yes | "test(admin): stop the audit log from becoming editable" — append-only was a docstring CONVENTION; it is now enforced by a source guard (no update/delete against AdminAuditLog, no PATCH/PUT/DELETE audit route), plus SET-NULL-not-CASCADE on the actor so an administrator cannot erase their trail by deleting their account. NOT a database-level guarantee (no trigger, no revoked GRANT) — recorded as such. Retention and export remain open |
| SUPPORT-001 | HIGH | Support tickets | EMAIL-001, PRIV-001 | IMPLEMENTED | Create, acknowledge, status, assignment, escalation and access tests | YES | NO | Yes | Yes | "feat(support): give staff somewhere to track support work" — migration 0069. INTERNAL ONLY: staff log requests that arrive by email; deliberately NO customer-facing intake, attachments, threads, SLA, priority or CSAT (a scope-guard test asserts those columns stay absent). Reuses admin permissions (new "support.tickets" key) and the append-only audit log. No account-mutation actions: the ledger asked for ticketing, assignment and escalation only |
| LEGAL-001 | CRITICAL | Production legal copy | PRIV-001 | INVENTORY_COMPLETE | Required section/link/version inventory and rendering tests | NO | NO | Yes | Yes | docs/LEGAL_SURFACE_INVENTORY.md — inventory only, per scope decision. Classifies every surface as TECHNICAL CONTROL / NEEDS LEGAL COPY / NEEDS PRODUCT DECISION / NEEDS COUNSEL REVIEW, names each third-party processor with what actually crosses the boundary, and lists the eight questions engineering must not answer. The version-provenance gap it found is now CLOSED ("feat(legal): make a published legal version an immutable snapshot"): wording moved verbatim into lib/legal/versions/2026-06-01.ts, pages render from the registry and display the version, and a checksum test fails if a published archive is edited. Superseded versions ARE reader-reachable: /terms/{version} and /privacy/{version} render the named immutable version, noindex, canonical to the unversioned page, 404 on an unknown version, absent from the sitemap ("feat(legal): let a superseded version still be read"). The WORDING itself is LEGAL-002, BLOCKED_EXTERNAL |
| LEGAL-002 | CRITICAL | Counsel approval | LEGAL-001 | BLOCKED_EXTERNAL | Recorded India-first counsel approval and effective-date verification | NO | YES | Yes | Yes | — |

## Phase 10 — CI/CD and production platform

Locally complete and certified, and the statuses here are deliberately uneven because the evidence is. Certification totals are in the handoff under "Phase 10 certification".

`VALIDATED` — PLATFORM-002 and PLATFORM-005 only. Their required tests exist, run here, and pass: the configuration boot matrix is exercised by 89 cases that mutate one setting at a time against a configuration that otherwise boots, and the health contracts are proven behaviourally and structurally, both with mutation checks.

`IMPLEMENTED`, not elevated, and each for a stated reason rather than caution: CI-001 and CI-002 encode the gates and are locally validated as workflows — YAML parses, every script and path exists, the Postgres image and downgrade target are pinned to the local harness by tests — but no GitHub runner has executed them, and the tests-required column asks for runs *in CI*. PLATFORM-001 asserts the container's properties from its source; the image has never been built, because Docker is unavailable here. PLATFORM-003's local half is validated (sole-head refusal, the already-at-head no-op, failure exit codes, lock-key symmetry) while the half named in its tests-required column — a fresh PostgreSQL upgrade and real two-process lock contention — has not run anywhere. PLATFORM-004 predates this certification pass and was not re-verified in it.

The honest summary: Phase 10 is done to the edge of this environment. Everything past that edge is RELEASE-001, and it is `BLOCKED_EXTERNAL` rather than pending.

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| CI-001 | CRITICAL | Required checks | Phases 1–9 | IMPLEMENTED | CI dry runs for type, lint, unit, backend, Postgres, browser and build | NO | YES | Yes | Yes | `.github/workflows/ci.yml` — five jobs: frontend (tsc/lint/unit/build), production dependency audit, backend (ruff/pytest+junitxml/single-alembic-head plus exact Redis 7.4.11 atomic contention/expiry/outage exercise), backend-postgres (postgres:16-alpine matching the local harness; up/down/seed/up then the PostgreSQL suites), browser (Playwright then QA SEQUENTIALLY — shared `.next` and fixed ports). Job boundaries follow state, not speed. YAML is validated and every referenced script/path/service version is pinned by `tests/ciWorkflowContract.test.mjs`; the Redis command was additionally executed against a real local Redis 7.4.11 service. GitHub-hosted execution remains `BLOCKED_EXTERNAL` because nothing was pushed — see `.github/workflows/README.md` |
| CI-002 | HIGH | Security/supply chain | DEP-003 | IMPLEMENTED | npm/Python audits, Gitleaks, SBOM and image scan in CI | NO | YES | Yes | Yes | `.github/workflows/security.yml` runs Gitleaks on full history, frontend and backend production-only audits, CycloneDX SBOM generation, image build/scan and container source assertions. Phase 10E-2 remediated the real Prisma peer-installed deepmerge-ts chain with an exact production override rather than a semver-major downgrade or suppression; frontend production audit now has zero analysed and zero unanalysed high/critical findings. Phase 13B fixed a second real gate defect: `uv run --with pip-audit` had resynchronized the developer group, so pytest/Pygments could fail a gate labelled production-only. CI now calls one tested script which exports the committed `--no-dev` lock with hashes, invokes pinned pip-audit 2.10.1 and requires every finding to match an exact evidence-bound allowlist; new/changed/stale findings fail and there is no blanket ignore. Local workflow contracts and audit/seed tests pass; the production audit exits zero with one analysed ecdsa residual and zero unanalysed findings. The redis-py addition makes the backend hashed export 55 requirement rows; frontend SBOM remains 39 components. Remote GitHub execution, full-history Gitleaks and built-image scanning remain external |
| PLATFORM-001 | CRITICAL | Production container | DEP-003 | IMPLEMENTED | Non-root, prod-only deps, immutable build and health-check tests | NO | YES | Yes | Yes | "harden(container): stop the image running as root and shipping the working directory" — non-root uid 10001 with nologin, USER before CMD, new .dockerignore (secrets first), --chown copy, bounded liveness HEALTHCHECK on /api/v1/health. Locked prod-only deps predate this. 22 source-level tests. The actual image BUILD and SCAN remain BLOCKED_ENVIRONMENT (no Docker) |
| PLATFORM-002 | CRITICAL | Configuration contract | WEB-008, RATE-001, MEDIA-001, EMAIL-001 | VALIDATED | HTTPS/origin/CORS/host/cookie/secret/service/flag boot matrix | NO | YES | Yes | Yes | "feat(config): classify every setting for production, and enforce what that claims" — `app/core/config_contract.py` classifies all 71 Settings fields as CORE_REQUIRED (33) / FEATURE_CONDITIONAL / OPTIONAL_DEVELOPMENT / TEST_ONLY with the enforcement backing each. The load-bearing rule is that CORE_REQUIRED implies enforcement is BOOT or SCHEMA: every such entry names a value that must be refused, and the test sets it on a configuration that otherwise boots and demands a RuntimeError naming the env var — so a classification cannot be satisfied by a comment. Applying it found THREE documented requirements with nothing behind them: REALTIME_BUS (its startup refusal had one caller, the health probe, so multi-instance production ran process-local realtime unacknowledged and silently), SMTP_USE_TLS (false sent the mail password and every reset link in the clear), LOG_LEVEL (DEBUG enables SQLAlchemy statement logging with bound parameters — addresses, tokens, hashes — to stdout). All three now refuse; non-vacuity proven by disabling all three at once (4 cases failed) and restoring. Adds a schema-validated LOG_LEVEL name, since `WARN` previously resolved to INFO via a getattr default. `python -m scripts.print_config_contract` renders the operator checklist from the registry and is asserted to read no values. Backend 7,369 → 7,458 tests, 0 failures. NOT DONE: backend/.env.example is permission-denied here, so the env template does not yet mention the three new production requirements |
| PLATFORM-003 | CRITICAL | Migration release step | CI-001 | IMPLEMENTED | Single-run fresh Postgres upgrade and compatible rollout tests | NO | YES | Yes | Yes | "feat(release): make migrations a release step instead of a race" — migrations previously ran from the container START command, so every instance executed `alembic upgrade head` on boot; alembic takes no lock, so concurrent DDL either deadlocks or errors, and `set -e` crash-loops the loser. `scripts/release_migrate.py` checks for a sole head BEFORE opening any connection (two heads is a merge accident), serialises on a PostgreSQL advisory lock polled with a deadline and released in a `finally`, then upgrades and reports the revision before/after including the already-at-head no-op. Never seeds, never starts the app, exits non-zero with one line and no traceback. `start_render.sh` and the backend-postgres CI job both migrate through it — CI twice, covering the no-op path — asserted from both sides so CI cannot drift back to raw alembic. VERIFIED LOCALLY: no-op exit 0 without invoking alembic, failure exit 1, two-heads refusal (monkeypatched, non-vacuous), ordering/lock-key symmetry/finally-release/dialect gating. NOT VERIFIABLE HERE: the full chain needs PostgreSQL (migration 0001 uses JSONB) and `docker` is permission-denied, so a fresh-database upgrade and REAL two-process lock contention are covered only by the CI job, which has never run remotely. Still open and better: a genuine pre-deploy hook — Render Free has none, and the release step is now one idempotent command, so adopting one is platform configuration rather than code |
| PLATFORM-004 | HIGH | Database pool safety | PLATFORM-002 | IMPLEMENTED | Pool-limit, acquire/statement timeout and saturation tests | NO | YES | Yes | Yes | "feat(db): bound the connection pool in code, and make production state its capacity" — the CODE guarantees boundedness (finite pool, bounded acquire wait, recycle); the DEPLOYMENT supplies DB_POOL_SIZE and DB_MAX_OVERFLOW, and production refuses to boot without them. NO baked-in capacity default, per explicit decision: "20 because that seems normal" exhausts a small managed plan once eight processes share it, and the symptom is latency rather than an error. SQLite gets no pool options at all |
| PLATFORM-005 | HIGH | Health contracts | PLATFORM-002 | VALIDATED | Liveness/readiness dependency and secret-redaction tests | NO | YES | Yes | Yes | "feat(health): separate readiness from liveness, and keep both quiet" — there was NO readiness probe; `/health/db` was the closest thing and is the wrong shape, since taking the request-scoped session means an unreachable database fails during dependency resolution and answers 500 rather than 503. `GET /health/ready` consults only the database, returns 503 when it is unreachable, and never consults an optional feature — a liveness probe that consults a dependency restarts every container during a blip, and a readiness probe that consults a feature drops every instance out of rotation over AI job import. Both asserted behaviourally AND structurally, the latter because a `Depends` added to liveness later breaks no behavioural test. Redaction is parametrised over every probe with recognisably-shaped secrets (DSN with password, SMTP credentials, Redis URL with inline auth) plus a guard proving the check can fire. Feature probes stay 200 always. Dockerfile HEALTHCHECK still points at liveness, now asserted. FOUND AND FIXED en route: the probe's own session used `settings.database_url`, which under APP_ENV=test is the developer's dev.db — same shape as the earlier webhook/dev.db escape and equally invisible from a green run; fixed with a dependency returning a session FACTORY (cannot fail, so 503 survives; overridable, so conftest points it at TestSessionLocal), verified by md5 of dev.db before/after. Non-vacuity proven by mutation (4 cases fail). Backend 7,471 → 7,499 tests, 0 failures |
| RELEASE-001 | CRITICAL | Branch promotion | BASE-004, CI-001 | BLOCKED_EXTERNAL | Authenticated remote fetch, protected-main PR and required checks | NO | YES | Yes | Yes | — |

## Phase 11 — Metadata, SEO, performance, and accessibility

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| SEO-001 | HIGH | Canonicals/noindex | BASE-003 | VALIDATED | Public self-canonical; private/auth/admin/error noindex tests | NO | NO | Yes | Yes | "fix(seo): stop robots.txt from hiding the noindex it was meant to reinforce" — robots.txt disallowed /auth/, /you/, /admin/, /dev/ and /smart-typing-test while NONE of them carried a noindex, which is the inverse of what it looks like: disallow stops the fetch, and a crawler must fetch a page to read its noindex, so the disallow guaranteed the directive could never be read while a linked URL could still be listed as a bare link. Now robots.txt blocks only /api/ (JSON, no meta tag possible, so Disallow is the only tool and its weakness is knowingly accepted) and every authenticated surface carries `noindex, nofollow` itself — 31 server pages page-level, plus /auth, /post-job and /post-talent via a layout because a client component cannot export metadata. `lib/seo/routeIndexing.ts` classifies all 45 routes with a reason; an unclassified `page.tsx` fails a test, as does a stale entry. FOUND WHILE CLASSIFYING: /u/[slug]/projects/[projectId] had NO metadata at all, so every public portfolio project presented the site-wide title with no description or canonical — now derived from the project, with the not-found/moved cases noindexed rather than titled optimistically; self-canonicals added to /faq, /support, /terms, /privacy. Non-vacuity proven by mutation, which exposed a VACUOUS assertion: one test scanned robots.ts for quoted paths and read the explanatory comment naming /you/ and /admin/ as configuration, so it passed with the defect present; rewritten to parse the array literal, with a guard on the parse. node tests 1,204 → 1,214 |
| SEO-002 | HIGH | Sitemap | SEO-001 | IMPLEMENTED | Pagination, real timestamps, failure reporting and coverage tests | NO | YES | No | Yes | "fix(seo): stop the sitemap lying about what exists and when it changed" — four defects, each producing a valid file, which is why none was noticed. (1) TRUNCATION: one request for 100 records, so with 400 open jobs 300 were absent; now walked to the end with three stopping conditions (short page, reported total, the protocol's 50,000-URL ceiling) because a server ignoring `offset` would loop on page one. (2) FABRICATED TIMESTAMPS: every job carried `lastModified: new Date()`, claiming the whole platform changed when the crawler asked — worse than omitting it, since that teaches crawlers to disregard `lastmod` for the site; now from `updated_at`/`created_at`, with an unparseable value yielding nothing rather than today. (3) `.catch(() => [])` published an outage as a fact: an unreachable backend produced a six-page sitemap with a 200, which a crawler may believe; the error now propagates to a 500. (4) FOUND BY FIXING (3) — the sitemap was PRERENDERED AT BUILD TIME, so the fetch failure had been happening at build and being absorbed; a sitemap of live jobs frozen at deploy misses everything posted after it, and a backend-less build shipped a permanently-empty file. Now `force-dynamic`, confirmed by the build output. Coverage is compared against `lib/seo/routeIndexing.ts` in both directions (/faq was indexable and absent). Non-vacuity proven by reintroducing all three original defects (3 cases fail). NOT PROVEN: these read source, not behaviour — pagination against a real API and a live crawler seeing a 500 need a running backend, so the row stays IMPLEMENTED rather than VALIDATED. node tests 1,214 → 1,226 |
| SEO-003 | HIGH | JobPosting lifecycle | WEB-007, SEO-001 | VALIDATED | Schema validity, expiry, closure and removal tests | NO | NO | Yes | Yes | "fix(seo): stop closed jobs advertising themselves as open" — the page emitted a full `JobPosting` block for every job it could load, whatever its status, so a closed/archived/paused/draft role kept making a machine-readable claim that it was open and applyable, indefinitely and with no expiry to contradict it; aggregators act on that, which is what Google's remove-on-close requirement and its manual actions exist for. Second defect: `validThrough` came from `deadlineAt`, so a published job PAST its own deadline still published as open — status alone cannot decide this. `lib/seo/jobPostingLifecycle.ts` holds ONE rule consulted from both `generateMetadata` and the page body, because duplicating it eventually produces a page that noindexes itself while still emitting an open JobPosting (search drops the page, the aggregator keeps the listing); a test asserts both call sites and forbids a hand-rolled status check. `featured` counts as open, `paused` does not (the vocabulary cannot express a pause), an unknown status fails closed, an unparseable deadline does NOT delist a live job, and the clock is a parameter so expiry is testable. Added `directApply: true` (true by design — applications never leave the platform). `baseSalary` deliberately NOT emitted, with a test enforcing its absence: exact-source compensation semantics are TRUST-001 and unresolved. 13 of 16 cases are behavioural against the imported rule; non-vacuity proven by widening OPEN_STATUSES (5 cases fail). node tests 1,226 → 1,242 |
| SEO-004 | MEDIUM | Profile/project metadata | SEO-001 | VALIDATED | Title/description/canonical/structured-data tests | NO | NO | No | Yes | Two parts. The project page was the SEO-001 finding: `/u/[slug]/projects/[projectId]` had NO metadata at all, so every public portfolio project presented the site-wide title with no description and no canonical; it now derives title/description/canonical/OpenGraph/Twitter from the project and noindexes the not-found, moved and missing cases rather than titling them optimistically. Second part, found by auditing the talent surface: talent listings share the job status vocabulary (`draft|published|paused|closed|archived|featured`) and had NO lifecycle check, so a recruiter could search, find a listing, and reach a creator who had taken it down. `talentListingVisibility` now gates indexability — and deliberately does NOT consult `availability_status`, which is the distinction that makes it a separate function rather than a reuse: "unavailable" means busy, not gone, and delisting on it would hide real people over a field they flip weekly while a recruiter planning next quarter still has reason to find them. A structural test asserts the talent rule reads no availability field, non-vacuity proven by making it do so (2 cases fail). `/u/[slug]` metadata was already sound and noindexes a hidden or missing profile. node tests 1,242 → 1,251 |
| PERF-001 | HIGH | Images/LCP | MEDIA-004 | VALIDATED | Image optimization, responsive dimensions and LCP measurements | NO | YES | Yes | Yes | `perf(images): optimize only owned media` completes the trust split. The server optimizer receives exactly the canonical `MEDIA_PUBLIC_BASE_URL` plus the backend-owned `avatars/{32hex}/{16hex}.{gif,jpg,png,webp}` and `banners/...` key shapes: eight extension-specific patterns, no wildcard host, no redirects, no local IP, no SVG and a 10 MiB response cap. An exact stored-key classifier gates the only remote `next/image` call sites; arbitrary creator-linked media remains browser-fetched. Every raw image has an explicit eager/lazy and async-decoding decision, while responsive `sizes` are supplied for optimized media. Live production-artifact tests prove four untrusted inputs are refused and third-party profile media never enters `/_next/image` (2/2). Phase 11F measured actual LCP elements at 1440x900 on `/`, `/jobs`, `/jobs/1`, `/talent`, `/talent/mock-talent-retention-editor` and `/u/aarav-mehta`; all were text. No numeric timing is claimed because unrelated host load was 48–61. Frontend node 1,257/1,257, focused security 28/28, TypeScript/build green |
| PERF-002 | HIGH | Workspace scale | CORRECT-007 | VALIDATED | Pagination, bundle and 100-concurrent-session load tests | YES | YES | Yes | Yes | Five measured slices are complete. Navigation fan-out is removed; activity enrichment stays under an invariant 18-SELECT ceiling where the N+1 baseline reached 298; the bundle report measures real cold entries; 100 concurrent encrypted sessions preserve 100 identities through session and workspace renders; and `perf(activity): paginate the workspace timeline` bounds the payload itself at 100 by default/200 maximum. Migration 0070 adds the four viewer + updated_at + id indexes. Static-tie, three-page completeness, ownership, malformed/wrong-mode cursor, exact-count, context, deep-link, rollout-fallback and browser paging tests are green. Production datastore/latency/capacity validation remains an infrastructure gate and is not inferred from local correctness |
| A11Y-001 | HIGH | Automated accessibility | Phases 1–9 | VALIDATED | Axe across route/state/viewports plus Firefox and WebKit | NO | YES | Yes | Yes | `fix(a11y): gate customer surfaces across browsers` pins direct `axe-core` 4.13.0 and adds a fail-closed whole-document WCAG 2.0/2.1/2.2 A/AA matrix over 19 public routes/states, modal states, Applications Inbox/filter/conversation/Pipeline, 320 px reflow, 200%-equivalent zoom and reduced motion in Chromium, Firefox and WebKit. Phase 13B supplied the previously missing canonical aggregate proof: 87/87 in 9.6 minutes, exactly 29/29 on each engine in one run. Real-backend accessibility QA is 8/8. Product fixes cover names, focusable horizontal review navigation, 24 px inline targets, 44 px primary targets, collapsed navigation semantics, stable row-state height and explicit decorative/status semantics. CI keeps standard, real-backend QA and accessibility sequential so they cannot race one `.next` artifact. Frontend Node is now 1,291/1,291; TypeScript/build are green; lint has 0 errors/32 existing warnings; production dependency audit has zero frontend high/critical and zero unanalysed backend findings. Manual assistive-technology review remains A11Y-002 and is not inferred from automation |
| A11Y-002 | HIGH | Manual accessibility | A11Y-001 | BLOCKED_EXTERNAL | Keyboard, screen reader, zoom, contrast, motion and touch review | NO | YES | Yes | Yes | — |

## Phase 12 — Recovery, observability, and operational readiness

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| OPS-001 | CRITICAL | Database backup/PITR | PLATFORM-003 | BLOCKED_EXTERNAL | Encrypted backup policy and isolated restore meeting RPO/RTO | NO | YES | Yes | Yes | — |
| OPS-002 | HIGH | Media backup/lifecycle | MEDIA-005 | BLOCKED_EXTERNAL | Provider retention and restore validation | NO | YES | Yes | Yes | — |
| OPS-003 | CRITICAL | Error reporting | PLATFORM-002 | VALIDATED | PII scrubbing, ingestion failure and source-map tests | NO | YES | Yes | Yes | `ops(errors): add privacy-bounded failure reporting` — React route/global/window/rejection capture sends one anonymous, rate-limited DTO containing only a finite boundary/type/digest/release plus at most 12 canonical Next chunk coordinates; messages, page URLs, identities, auth context and credentials are not in the schema. FastAPI emits the same finite `error_event` shape for browser and backend failures to structured stdout, while the process-wide JSON formatter never interpolates logging arguments and reduces exception data to type plus module-relative coordinates. Sink failure cannot replace the safe 500 or create a browser rejection. Release builds archive 454 checksummed client/server maps under an immutable release, then prove zero `.map` files and zero map hints remain web-addressable; CI retains the private artifact for 30 days. Focused frontend 6/6, backend 5/5, real release build green, frontend aggregate 1,276/1,276, authoritative backend aggregate 7,506 collected/exit 0, deterministic clock boundary 11/11, TypeScript and changed-file Ruff green. Whole-backend Ruff still has 67 pre-existing findings in files untouched by this slice and remains a CERT-001 blocker. Live aggregation/retention and alert delivery remain OPS-005/CERT-002 rather than being inferred from local stdout |
| OPS-004 | HIGH | Metrics/tracing | OPS-003 | VALIDATED | HTTP, DB, Redis, email, realtime, upload, auth and AI telemetry tests | NO | YES | Yes | Yes | `ops(metrics): emit privacy-bounded operational signals` — one vendor-neutral `metric_event` schema reaches API and standalone workers through their existing structured stdout transport. Seven finite metric names cover HTTP/status/duration, DB probes, Redis limiter allowed/rejected/unavailable, email pass/delivery outcomes, realtime publish and AI provider calls; auth, avatar/banner upload and AI routes get finite subsystem labels from matched framework templates, never raw URLs. The formatter independently allowlists every label/number and discards arbitrary fields. Request UUID correlation works without a tracing provider; caller request IDs are accepted only as canonical UUIDs so emails/tokens/prose cannot become log content. Sink failure cannot alter a response/write. Every schema name has a real producer and one formatter consumer, guarded structurally and behaviourally. New telemetry 14/14; telemetry/error security 19/19; affected subsystems 236/236; auth/request-ID regression 104/104; changed-file Ruff green. The later RATE-001 slice closes the separate non-atomic implementation/real-broker proof gap while retaining these bounded allowed/rejected/unavailable metrics. Live aggregation, retention and alert delivery remain OPS-005/CERT-002 |
| OPS-005 | CRITICAL | Alerts/synthetics | OPS-004 | BLOCKED_EXTERNAL | Auth, browse, publish, apply, message and admin synthetic tests | NO | YES | Yes | Yes | `ops(alerts): add finite policies and local synthetics` supplies a provider-neutral evaluator over only finite privacy-bounded error/metric/static-message contracts. Every actionable producer maps to one static severity/summary/runbook key; no customer/provider/request content is copied, one batch is deduplicated and an actionable result exits nonzero. Six existing real-backend QA journeys cover auth, browse, deterministic no-provider publish, apply, message and admin under the serial disposable-SQLite scenario harness. Phase 13B clears the last local gate: all six passed together in one serial aggregate in 2.7 minutes, with no timeout/assertion relaxation. Alert tests are 8/8, selector contract 3/3 and selector listing 6/6. Local implementation and deterministic proof are complete. Live log ingestion/retention/access, destination delivery and acknowledgement, standalone-worker/process-death detection, scheduled isolated-staging execution, evidence-backed thresholds and soak require real infrastructure and remain external |
| OPS-006 | HIGH | Incident runbooks | OPS-003 | VALIDATED | Tabletop review for rollback, compromise, data loss, abuse and providers | NO | YES | Yes | Yes | `ops(runbooks): make incident containment executable` — CreatorJobs-specific response paths now cover bad release, bad migration, customer/admin compromise, database/media loss, rate-limit abuse, email, AI, realtime/shared-state and Google/YouTube incidents. Every path separates detection, containment, privacy-safe evidence, recovery, verification and the exact external stop; no maintenance mode, downgrade shortcut, backup success, provider failover or production threshold is invented. A recorded no-provider repository tabletop covers all five required classes and explicitly is not a human/live drill. Machine checks require every named application control to have executable declaration, enforcement and an operator consumer; preserve liveness/readiness/optional-feature separation; keep destructive shortcuts/credentials out of command blocks; and pin OPS-001/002 external truth. Review found and fixed one concrete containment gap: sanctioned `grant_admin --revoke` demoted the account but left durable sessions alive. It now locks/demotes the user, revokes every durable session/refresh credential and writes a bounded actor-less audit event atomically; migration-era claimless access remains bounded and must be suspended or globally invalidated. Operator docs now also match the shared email outbox: `EMAIL_DELIVERY_ENABLED=false` gates real auth and notification sends only after worker restart while intents remain durable. Runbook/tabletop contract 5/5; emergency revoke/grant 2/2; audit-integrity focus 15/15; configuration/operator-contract focus 120 passed/1 skipped; cross-subsystem incident matrix 281/281; changed Python Ruff green. Hosted rollback, provider, backup, traffic, human on-call and credential drills remain external under OPS-001/002/007/008 and CERT-002 |
| OPS-007 | HIGH | Credential rotation | OPS-006 | VALIDATED | Rotation inventory and local/staging rehearsal | NO | YES | Yes | Yes | `ops(credentials): make secret rotation rehearsable` — `docs/CREDENTIAL_ROTATION.md` inventories 18 credential families with an issuing authority and real declaration/enforcement/consumer, separates planned overlap from compromise containment, and never promotes local re-encryption into provider revocation. A new dry-run-first `rotate_strong_auth_secrets` command authenticated-decrypts every TOTP factor, locks/rewraps bounded resumable batches under the active AES-GCM key ID, fails closed if an old key is missing, and emits counts/key ID only. The existing OAuth grant rewrap command is covered by the same machine contract. Planned Google credential-authority and email-webhook rotations now accept a bounded previous secret while producers switch, then reject it after retirement; production refuses missing/short frontend exchange secrets, short CreatorJobs-owned backend secrets, previous-without-current and same-value fake overlap. Emergency procedures retain no compromised previous value, and exposed TOTP seeds/provider grants require re-enrollment/revocation rather than cosmetic rewrap. Focused backend 165 passed/1 skipped; broad auth/OAuth/Google/email/config regression 600 passed/16 skipped; frontend/runbook/config 16/16; TypeScript/build and changed-file Ruff green; lint 0 errors/32 existing warnings. Live Google/provider/database/Redis/SMTP/hosting rotation, physical-factor challenge and hosted session observation remain `BLOCKED_EXTERNAL` under CERT-002 rather than being inferred from disposable local values |
| OPS-008 | HIGH | Staging soak | OPS-005, all beta blockers | BLOCKED_EXTERNAL | 72-hour soak with SLOs and no unresolved critical/high incident | NO | YES | Yes | Yes | — |

## Phase 13 — Final certification

| ID | Severity | Subsystem | Dependencies | Status | Tests required | Migration | Infra | Blocks beta | Blocks unrestricted | Resolving commit |
|---|---|---|---|---|---|---|---|---|---|---|
| CERT-001 | CRITICAL | Complete local matrix | All implementation items | IN_PROGRESS | Every prescribed frontend/backend/security/browser suite is green | NO | NO | Yes | Yes | `chore(certification): clear backend static gate` completed Phase 13A and Phase 13B (`fix(certification): harden browser and dependency gates`) repaired product/gate defects surfaced by browser certification. The RATE-001 closure adds 23 backend cases without regressing the aggregate: 7,499 passed/65 skipped/71 warnings; frontend Node is 1,292/1,292; whole-backend Ruff and the production dependency audit remain green, with one exact analysed structurally unreachable ecdsa residual and zero unanalysed findings. Real Redis 7.4.11 contention passed twice at exactly 17 allowed/183 rejected. TypeScript/build are green; ESLint has 0 errors/32 existing warnings; accessibility is 87/87 and synthetics 6/6 in canonical aggregates. Standard Playwright was 502 passed/2 host-load timeouts; both exact unchanged cases then passed, one 10/10 and the affected group 45/45. QA was 256 passed/35 host-contended failures/1 skipped over 1.9 hours; 22 passed focused unchanged, five reproduced as request/global time-budget exhaustion at load 32–57, and eight late persona timeouts still need quiet-host isolation. No timeout, sleep, assertion, security check or product validation was weakened. CERT-001 remains IN_PROGRESS pending a quiet-host QA aggregate plus unavailable Gitleaks/built-image scans and the rest of the prescribed focused matrix |
| CERT-001A | HIGH | Live-provider test opt-in | CERT-001 | VALIDATED | Exact1 is the only enabling value; actual test.skip AST regression fails on old string0 truthiness; fixed focused16/16, Node1316/1316, actualPlaywright0→1skipped. FullQA293/293 prior to correction included one unintended live smoke with blank API key and recoverable failure; not a live-provider success. All292 deterministic cases passed; a11y87/87; TSC/lint/build pass. See13C handoff for evidence correction | NO | NO | Yes | Yes | `test(certification): enforce explicit live import opt-in` |
| CERT-002 | CRITICAL | Production configuration | CERT-001, all external service items | BLOCKED_EXTERNAL | Real service boot, TLS, cookie, header, readiness and provider probes | NO | YES | Yes | Yes | — |
| CERT-003 | CRITICAL | Controlled cohort | OPS-008, CERT-002 | BLOCKED_EXTERNAL | Ten-account, seven-day cohort with rollback triggers and SLO review | NO | YES | Yes | Yes | — |
| CERT-004 | CRITICAL | Release assessment | CERT-001, CERT-002, CERT-003, LEGAL-002 | NOT_STARTED | Evidence review using exact status semantics | NO | YES | Yes | Yes | — |
